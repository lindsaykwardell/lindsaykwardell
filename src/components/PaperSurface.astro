---
interface Props {
  class?: string
  plain?: boolean
}

const { class: className, plain = false } = Astro.props
---

<div
  class:list={[
    'paper-surface prose prose-lg max-w-none bg-background rounded',
    plain && 'paper-surface--plain',
    className,
  ]}
>
  <slot />
</div>
